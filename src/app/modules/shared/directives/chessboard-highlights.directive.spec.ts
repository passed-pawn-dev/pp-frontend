import { ChessboardHighlightsDirective } from './chessboard-highlights.directive';

describe('ChessboardHighlightsDirective', () => {
  it('should create an instance', () => {
    const directive = new ChessboardHighlightsDirective();
    expect(directive).toBeTruthy();
  });
});
