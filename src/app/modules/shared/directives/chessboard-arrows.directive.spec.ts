import { ChessboardArrowsDirective } from './chessboard-arrows.directive';

describe('ChessboardArrowsDirective', () => {
  it('should create an instance', () => {
    const directive = new ChessboardArrowsDirective();
    expect(directive).toBeTruthy();
  });
});
