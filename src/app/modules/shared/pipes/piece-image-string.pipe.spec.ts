import { PieceImageStringPipe } from './piece-image-string.pipe';

describe('PieceImageStringPipe', () => {
  it('create an instance', () => {
    const pipe = new PieceImageStringPipe();
    expect(pipe).toBeTruthy();
  });
});
